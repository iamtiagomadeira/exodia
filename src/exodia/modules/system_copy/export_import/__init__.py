"""Export/Import system copy — SWPM-driven (R3load for ABAP, JLoad for Java).

The database export/import method uses SWPM to export the source database into
platform-independent load files and import them into a freshly installed target.
It is the mandatory method when the DB or OS changes (heterogeneous copy /
migration) and the only supported method for AS Java (JLoad).
"""
