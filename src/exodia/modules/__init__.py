"""Pluggable methodology modules (the 'Limbs'). Drop-in: auto-discovered by the registry.

Each subpackage (e.g. backup_restore, pipo, hsr) contributes Check and Action
subclasses that the registry finds automatically.
"""
